# Em desenvolvimento isso não é necessário — o Vite já faz proxy de /api
# para http://localhost:4000 (ver vite.config.ts).
#
# Em produção, aponte para a URL pública do backend, por exemplo:
# VITE_API_BASE_URL=https://sua-api.exemplo.com/api
VITE_API_BASE_URL=
