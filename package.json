{
  "name": "ai-website-hunter",
  "private": true,
  "version": "1.0.0",
  "description": "AI Website Hunter — encontre empresas sem site e gere um site profissional para elas.",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -k -n BACKEND,FRONTEND -c blue,green \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "build": "npm run build -w backend && npm run build -w frontend",
    "start": "npm run start -w backend",
    "lint": "npm run lint -w frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
